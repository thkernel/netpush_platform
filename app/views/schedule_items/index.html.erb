<div class="br-pageheader">
      <nav class="breadcrumb pd-0 mg-0 tx-12">
        
        <span class="breadcrumb-item active"><h5>Horaires d'ouverture</h5></span>
      </nav>
    </div><!-- br-pageheader -->
<div class="br-pagebody">
  <div class="br-section-wrapper">
    <div class="headers mg-b-20">
        <div class="left-content">
        </div>

        <div class="text-right">
            <%= link_to raw('<i class="fa fa-plus" aria-hidden="true"></i> Ajouter'), new_schedule_path, class: "btn btn-primary tx-mont tx-medium tx-11 tx-uppercase pd-y-12 pd-x-25 tx-spacing-1", remote: false %>

        </div>

    </div>


<table id="datatable1" class="table display responsive nowrap">


  <thead>
    <tr>
      <th>Jours</th>
      <th>Heure début</th>
      <th>Heure fin</th>
    </tr>
  </thead>

  <tbody>
    <% @schedule_items.each do |schedule_item| %>
      <tr>
        <td><%= schedule_item.day %></td>
        <td><%= schedule_item.start_time.strftime("%H:%m") %></td>
        <td><%= schedule_item.end_time.strftime("%H:%m") %></td>
        
      </tr>
    <% end %>
  </tbody>
</table>

</div>
</div>
<div id="speciality-modal" class="c-modal modal fade" data-backdrop="static"></div>
