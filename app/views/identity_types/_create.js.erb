$("ul.errors").html("");
<% if @identity_type.errors.any? %>
  <% @identity_type.errors.full_messages.each do |message| %>
 
    $("ul.errors").append($('<div class="c-alert c-alert--danger" > ').html("<%= message.html_safe %> "));
  <% end %>
<% else %>
  $(".identity-types").html("<%= escape_javascript(render 'index') %>");
  $("#identity-type-modal").modal("hide");
 
<% end %>